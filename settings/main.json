{
  ".text.html.php.blade": {
    "autocomplete": {
      // Blade components and Livewire/Alpine attributes are hyphenated —
      // `<x-alert>`, `wire:click`, `x-data` — so a hyphen has to count as part
      // of a word or every completion stops at the dash.
      "extraWordCharacters": "-"
    },
    "bracket-matcher": {
      "autocompleteCharacters": ["()", "[]", "{}", "\"\"", "''"]
    },
    "editor": {
      "commentStart": "{{-- ",
      "commentEnd": " --}}",
      "commentDelimiters": {
        "block": ["{{--", "--}}"]
      }
    }
  }
}
